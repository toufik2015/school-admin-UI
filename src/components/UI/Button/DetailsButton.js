import React from "react";
import classes from "./DetailsButton.module.css";

const DetailsButton = (props) => {
  return <button className={classes["details-btn"]}>Details</button>;
};

export default DetailsButton;
